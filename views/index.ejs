<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/index.css">
</head>
<body>
    <h1 class="quora-heading">Quora Posts</h1>
    <a class="createBtn" href="http://localhost:8080/posts/new">Create post</a>
    <% for(let post of posts) {%>
        <div class="post">
            <h3 class="username"> @ <%= post.username %></h3>
            <h4 class="content"><%= post.content %></h4>
            <div class="changes-div">
                <a class="view" href=<%= "http://localhost:8080/posts/" + post.id %>> View in detail </a>
                <a class="edit" href=""> Edit Post</a>
            </div>
            <button class="delete-btn">Delete Post</button>
        </div>
    <% } %>
    
    
    <script src=""></script>
</body>
</html>